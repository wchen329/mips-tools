#include <stdio.h>
#include <stdlib.h>

/* A simple MIPS assembler
 * By default does not write executable header,
 * only assembles each instruction into corresponding binary.
 *
 * wchen329
 */
int main()
{

	return 0;
}
